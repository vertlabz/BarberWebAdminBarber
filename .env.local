# Prefer leaving this empty to use same-origin "/api/*" (Next rewrites will proxy to the backend).
# If you run this admin WITHOUT the Next proxy, you can set it to e.g. http://localhost:3000
NEXT_PUBLIC_API_BASE_URL=

# Optional: configure the proxy target for Next rewrites.
# BACKEND_URL=http://localhost:3000